import { colors } from "./colors";

export const webDateInputBase: React.CSSProperties = {
  border: `1px solid ${colors.border}`,
  borderRadius: 8,
  padding: 12,
  marginBottom: 8,
  fontSize: 16,
  backgroundColor: colors.bgBase,
  fontFamily: "inherit",
};
